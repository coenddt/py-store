# py-store 测试报告

日期：2026-09-12
范围：宿主层（src/py_store）+ 真实后端 E2E + 多 db 联合压测

## 1. 本轮变更内容

| 提交 | 内容 |
|---|---|
| `c44001e` | 宿主接入守卫：`timestamps` 支持 `'s'` 秒级（镜像 `timestamp_unit`，写路径按单位注入秒/毫秒时间戳，非法值注册即报错）、`setAllowUserPipeline` 透传 core 开关、`feedback` 反馈事件通道（sink + emit，联邦 degraded 与 SQL 下推拒绝统一接入） |
| `28b79e0` | 1.0.0 多数据源 Host 适配（rebase 重放完成，此前中断的 4 个文件冲突——`pyproject.toml`、`crud/mutation.py`、`crud/write.py`、`datasource.py`——已全部解决） |

## 2. 测试结果

### 2.1 真实后端 E2E（tests/test_real_backends_e2e.py）

`LOCAL_CORE=1` 连接本机 MongoDB / MySQL / PostgreSQL / SQLite 四库真实实例：**全部通过**。

### 2.2 多 db 联合压测

四库并载 + 跨 3 源联邦压测（独立压测：8000 ops / 0 错误 / 824.35 QPS，联邦 p50 19.30 ms）详见 **[压测报告](./2026-09-12-压测报告.md)**（脚本 [stress/stress.py](../stress/stress.py)）。

## 3. 结论

1. **稳定性**：E2E 与压测累计 **42000+ 次操作 0 错误**；四库并载 + 跨源联邦链路无失败。
2. **正确性**：守卫功能（时间戳单位 / $pipeline 开关 / 反馈通道）与 1.0.0 多数据源定位在 E2E 与压测中行为符合预期。
3. **性能**：独立压测 824 QPS，三宿主（core 直压 727 / nodejs 638）中最高；瓶颈在本机单实例数据库侧。

## 4. 复现

```powershell
# E2E
$env:LOCAL_CORE='1'; $env:PYTHONPATH='src'; python -m pytest tests/test_real_backends_e2e.py -q

# 压测
$env:LOCAL_CORE='1'; python stress\stress.py --workers 8 --rounds 200
```
